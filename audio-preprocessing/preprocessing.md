Use librosa to load the audio file and convert it to a numpy array. Then, we can use the librosa.display.waveplot function to plot the waveform.

if you want to run the code:

`pip install librosa`
`pip install resampy`
`pip install pydub`
`pip install ffmpeg`

